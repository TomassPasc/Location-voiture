{% extends 'base.html.twig' %}

{% block title %}Création profile{% endblock %}

{% block monTitre %}Profile de {{app.user.pseudo}}{% endblock %}

{% block body %}

    {{form_start(form)}}
    {{form_widget(form)}}
    <button type="submit" class="btn btn-primary">Valider</button>
    {{form_end(form)}}

{% endblock %}

