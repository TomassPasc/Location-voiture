{% extends 'base.html.twig' %}


{% block title %}Location{% endblock %}

{% block monTitre %}Location {% endblock %}

{% block body %}

    <p>début location: {{reservations.date_debut|date("d/m/Y")}}</p>
    <p>fin location: {{reservations.date_fin|date("d/m/Y")}}</p>
    <p>nombre de jours réservé: {{reservations.jours}}</p>
    <p>Prix/jours: {{voiture.modele.prixMoyen}}</p>
    <p>Prix total: {{voiture.modele.prixMoyen * reservations.jours}}</p>


    {{form_start(form)}}

        {{form_widget(form)}}

      {# <button type="submit" class="btn btn-primary">Valider la réservation</button> #}
    {{form_end(form)}}

{% endblock %}

